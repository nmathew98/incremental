import { describe, it } from "vitest";

describe("makeMemo", () => {
	it.todo("throws if `fn` is not named");

	it.todo("memoizes the result of `fn`");

	it.todo("uses a custom `resolver` if one is specified to obtain the key");
});
